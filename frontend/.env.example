# API base URL (no trailing slash). Use full URL when backend runs on different host/port.
# Local:
VITE_API_URL=https://billing-software-aez3.onrender.com/api/v1
# Production (set in Vercel / your host):
# VITE_API_URL=https://your-backend-url.com/api/v1
